<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rudenia.fit.bstu.lab15">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab15">
        <activity android:name=".GroupComparison2Activity"></activity>
        <activity android:name=".FacultyComparisonActivity" />
        <activity android:name=".GroupComparisonActivity" />
        <activity android:name=".BadStudentsActivity" />
        <activity android:name=".BestStudentsActivity" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>